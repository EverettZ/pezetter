import { MaxLengthEtcPipe } from './max-length-etc.pipe';

describe('MaxLengthEtcPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxLengthEtcPipe();
    expect(pipe).toBeTruthy();
  });
});
